# dotfiles

my hyprland dots

## how to install

```sh
git clone https://github.com/iiiiiidev/dots ~/dots
cd ~/dots
./install.sh
```

if you already have the packages then
`./install.sh --skip-packages`
